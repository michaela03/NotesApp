<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotesApp"
        tools:targetApi="31">

        <!-- Основна активност за логин -->
        <activity
            android:name="com.example.notesapp.activities.LoginActivity"
            android:exported="true" />

        <!-- Основна активност на приложението -->
        <activity
            android:name="com.example.notesapp.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Активност за добавяне на бележка -->
        <activity
            android:name="com.example.notesapp.activities.AddNoteActivity"
            android:exported="true" />

        <!-- Активност за обновяване и изтриване на бележка -->
        <activity
            android:name="com.example.notesapp.activities.UpdateAndDeleteActivity"
            android:exported="true" />

    </application>

</manifest>
